# Cloudflare Pages が想定と違う Node でビルドしたときに、
# 気づかないまま進まず npm ci の時点で落ちるようにする。
#
# reform では Cloudflare が Node 12 でビルドし続けていたのに
# 古い成果物が配信され続け、7か月気づかなかった。
# engines.node を満たさなければ EBADENGINE で止まる。
engine-strict=true
